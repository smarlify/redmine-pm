#!/usr/bin/env ruby
# Ensure local server runs single-process by default (no Puma workers)
ENV["WEB_CONCURRENCY"] ||= "0"
APP_PATH = File.expand_path('../../config/application',  __FILE__)
require_relative '../config/boot'
require 'rails/commands'
